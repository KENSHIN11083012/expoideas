VITE_API_URL=http://localhost:8080/api/v1

# Subdirectorio donde se sirve la app (App.jsx lo usa como basename del router).
# Por defecto /expoideas/, igual que en Docker (ver Dockerfile). Para servir en
# la raíz durante el desarrollo local, descomenta la línea con un valor vacío.
# VITE_BASE_PATH=/
